#include "main.h"

/**
 * _islower - Checks for lower characters
 * @c: The character to be shecked
 * Return: 1 for lower characters or 0 for anything else
 */

int _islower(int c)

{
	if (c >= 97 && c <= 122)
	{

	return (1);
	}
	return (0);
}
